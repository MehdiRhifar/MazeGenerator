{
  "name": "pathfinding",
  "scripts": {
    "build:wasm": "cd backend && PATH=$HOME/.cargo/bin:$PATH wasm-pack build --target web && cd .. && rm -f backend/pkg/.gitignore",
    "dev:wasm": "cd backend && PATH=$HOME/.cargo/bin:$PATH cargo watch -s \"wasm-pack build --target web\"",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "npm run build:wasm && cd frontend && npm run build"
  }
}
